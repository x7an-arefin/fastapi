/**
 * Lifecycle Result — returned by the PROCESS handler.
 * Carries the output value and entity ID for use by the POST stage.
 */
export interface LifecycleResult {
  /** The value to return to the HTTP client */
  output: unknown;
  /** Primary key of the affected entity (null for list operations) */
  entityId: string | null;
}
